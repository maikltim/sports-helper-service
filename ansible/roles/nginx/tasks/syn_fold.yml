# task  for syn fold
- name: Copy client files to /var/www/sports-helper
  synchronize:
    src: "../../client"
    dest: "/var/www/sports-helper"
    delete: yes

- name: mkdir for web
  file:
    path: "/var/www/sports-helper"
    state: directory
    owner: nginx
    group: nginx
    mode: "0755"
  become: true

- name: Set ownership and permissions for www user
  file:
    path: "/var/www/{{ frontend_name }}"
    owner: nginx
    group: nginx
    mode: "0755"
    become: true

  notify:
     - Restart Nginx
